function total(arr) {
  return arr.reduce(function(total, num) {
    return total + num;
  });
}

function stringConcat(arr) {
  return arr.reduce;
}
var testArray = [1, 2, 3, 4, 5];

console.log(total(testArray));
